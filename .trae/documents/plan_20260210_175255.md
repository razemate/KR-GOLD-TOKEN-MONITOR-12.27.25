I will implement the requested changes, ensuring a backup is created first.

1. **Create Backup**:

   * Create a copy of `app/page.tsx` as `app/page.tsx.bak`.

2. **Set Light Theme as Default (`app/page.tsx`)**:

   * Update `toggleTheme` to persist the user's preference to `localStorage`.

   * Update the initialization `useEffect` to prioritize `localStorage` and default to **Light Mode** if no preference is saved. This removes the automatic system dark mode detection.

3. **Implement Domain Redirect (`middleware.ts`)**:

   * Create a new `middleware.ts` file to handle the redirection.

   * Add logic to check if the hostname is `gold.katusaresearch.com`.

   * If it matches, redirect the user to \`<https://subscribers.katusaresearch>.

